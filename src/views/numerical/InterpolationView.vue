<script setup>
import ConceptPage from '../../components/ConceptPage.vue'
import MathBlock from '../../components/MathBlock.vue'
import MathInline from '../../components/MathInline.vue'
import QuizBox from '../../components/QuizBox.vue'
import RevealBox from '../../components/RevealBox.vue'
import RungeDemo from '../../demos/RungeDemo.vue'
</script>

<template>
  <ConceptPage slug="interpolation">
    <h2><span class="sec-no">壹</span>困境：手里只有一张离散的表，中间的值怎么办</h2>
    <p>
      天文历表给出每天正午的行星位置，你想知道下午三点的；实验仪器每隔 0.1 秒采一个点，你想还原连续曲线；
      对数表只列了整数，你要 <MathInline tex="\lg 2.5" />。共同的处境是：只有<strong>有限个离散点</strong>，
      却想要它们之间的值。最自然的想法——找一条<strong>穿过所有已知点的光滑曲线</strong>，用它来"填空"。这就是<strong>插值</strong>。
    </p>
    <p>
      多项式是最顺手的填空工具：<MathInline tex="n+1" /> 个点，恰好唯一确定一条 <MathInline tex="n" /> 次多项式。
      拉格朗日（1795）给了一个漂亮的显式写法——为每个节点造一个"只在自己这儿等于 1、在别的节点都等于 0"的基函数：
    </p>
    <MathBlock tex="p(x)=\sum_{i=0}^{n} y_i\, L_i(x), \qquad L_i(x)=\prod_{j\ne i}\frac{x-x_j}{x_i-x_j}" />
    <p>
      于是自然而然地，人人都相信一条<strong>看起来天经地义的直觉</strong>：既然点越多、曲线被"钉"得越死，
      那用越高次的多项式、穿过越多的点，就该越贴近真相。整整一个世纪，没人怀疑这件事。
    </p>

    <h2><span class="sec-no">贰</span>破局（其实是打脸）：龙格 1901 年的反例</h2>
    <p>
      1901 年，<strong>龙格</strong>拿一个人畜无害的函数开刀：<MathInline tex="f(x)=\dfrac{1}{1+25x^2}" />，
      形状就是个温柔的钟形包。他在 <MathInline tex="[-1,1]" /> 上<strong>等距</strong>取点，用穿过这些点的多项式去逼近。
      结果惊掉了所有人：点取得越多、次数升得越高，多项式在<strong>区间两端的震荡越来越疯狂</strong>，
      振幅不是缩小而是<strong>发散到无穷</strong>。中间贴得越来越好，两端却彻底失控。
      "次数越高越准"这个百年直觉，被一个巴掌拍碎。
    </p>
    <div class="insight">
      <div class="insight-title">💡 深层真相藏在复平面里（复变第四讲的收割）</div>
      <p>
        <strong>这扇窗写给已经读过站内复变课的读者。还没读到也完全不影响后面</strong>——
        紧接着的那个折叠框只用插值余项公式（初等微积分的东西）就把龙格现象量了出来，
        也把切比雪夫点的来由从头讲清，全程不碰复变。这一框跳过去，一点债都不欠。
      </p>
      <p>
        表面看，龙格现象是"高次多项式太僵硬、被逼着穿点只好剧烈摆动"。但<strong>为什么偏偏是这个温和的函数</strong>？
        真正的解释要抬头看<strong>复平面</strong>。<router-link to="/complex/residue">复变第四讲</router-link>埋过一个伏笔：
        幂级数的<strong>收敛半径 = 到最近奇点的距离</strong>。龙格函数
        <MathInline tex="\dfrac{1}{1+25x^2}" /> 在实轴上处处光滑，可一旦延拓到复平面，它在
        <MathInline tex="x=\pm\tfrac{i}{5}" /> 处有两个<strong>极点</strong>——就藏在实轴上方不远处。正是这对看不见的奇点，
        卡住了等距插值的收敛半径，让两端的误差压不下去。一个纯实数的数值灾难，病根却在虚轴上——
        这是复变函数在本课最漂亮的一次现身。
      </p>
    </div>
    <p>
      "振幅发散到无穷"是句大话，得有数字撑着。而且顺着<strong>余项公式</strong>能看清
      切比雪夫点凭什么管用——它不是试出来的经验，是一道<strong>最优化问题的解</strong>：
    </p>
    <RevealBox
      title="🔍 把龙格现象量出来，再看清切比雪夫点为什么是最优的那一组"
      label="对答案 / 看完整拆解"
    >
      <template #hint>
        先看第肆节那条余项公式
        <MathInline tex="f - p = \frac{f^{(n+1)}(\xi)}{(n+1)!}\prod(x - x_i)" />。
        右边两个因子里，<strong>只有一个是你能控制的</strong>——
        <MathInline tex="f^{(n+1)}" /> 由函数自己决定，你改不了；
        可 <MathInline tex="\prod(x-x_i)" /> 完全由<strong>节点位置</strong>决定。
        那么"怎么取点最好"就变成了一道明确的题：<strong>选一组 <MathInline tex="x_i" />，
        让 <MathInline tex="\max|\prod(x-x_i)|" /> 尽可能小</strong>。先自己想想，
        等距取点时这个乘积在哪儿最大？
      </template>
      <p>
        <strong>第一步：先把灾难量出来。</strong>
        对龙格函数 <MathInline tex="1/(1+25x^2)" /> 在 <MathInline tex="[-1,1]" /> 上插值，
        全区间最大误差实测：
      </p>
      <MathBlock tex="\begin{array}{c|cc} \text{节点数} & \text{等距} & \text{切比雪夫} \\ \hline 11 & 1.92 & 0.132 \\ 21 & 59.8 & 0.0177 \\ 31 & \mathbf{2388} & \mathbf{0.00243} \end{array}" />
      <p>
        <strong>同一个函数、同样的节点数，一边误差涨到两千多，一边掉到千分之二。</strong>
        （注意函数本身的取值范围只有 0 到 1——误差 2388 意味着插值曲线在两端飞出了三个数量级。）
        <strong>"点越多越准"错得不能再错：等距情形下点越多越糟，而且是指数级地糟。</strong>
      </p>
      <p>
        <strong>第二步：责任在 <MathInline tex="\prod(x-x_i)" /> 这一项。</strong>
        等距节点时，取 x 落在最靠边的两个节点中间，那里到<strong>其余每一个</strong>节点的距离都很大，
        乘积自然大；而 x 落在区间正中时，两侧的因子有大有小，互相调和。
        <strong>于是这个乘积在两端鼓成大包、在中间被压平</strong>——
        这正是动画里大浪只在两端翻的原因。实测同样 21 个节点：
      </p>
      <MathBlock tex="\max\Bigl|\prod (x-x_i)\Bigr| : \quad \text{等距 } 2.34\times10^{-4}, \qquad \text{切比雪夫 } 1.90\times10^{-6}" />
      <p>
        <strong>第三步：那什么样的一组点能让这个最大值最小？</strong>
        这是切比雪夫 1854 年就解决的问题。答案是：把
        <MathInline tex="[-1,1]" /> 上的节点取成
      </p>
      <MathBlock tex="x_i = \cos\frac{(2i+1)\pi}{2n}, \qquad i = 0, 1, \dots, n-1" />
      <p>
        也就是<strong>把半圆周上的等分点投影到直径上</strong>——
        圆周上等距，投影下来自然两端密、中间疏。此时
      </p>
      <MathBlock tex="\max\Bigl|\prod(x-x_i)\Bigr| = 2^{\,1-n}" />
      <p>
        而且可以证明<strong>没有任何一组 n 个点能做得更小</strong>。
        实测验证：<MathInline tex="n = 11" /> 时算出
        <MathInline tex="9.77\times10^{-4}" />，而 <MathInline tex="2^{-10} = 9.77\times10^{-4}" />；
        <MathInline tex="n = 21" /> 时算出 <MathInline tex="9.54\times10^{-7}" />，
        <MathInline tex="2^{-20} = 9.54\times10^{-7}" />——<strong>一位不差地压在理论下界上</strong>。
        对比等距的 <MathInline tex="2.34\times10^{-4}" />，差了两百多倍。
      </p>
      <p>
        <strong>一个容易混淆的小字：切比雪夫点有两套。</strong>
        上面那组是切比雪夫多项式的<strong>零点</strong>，它精确达到最优
        <MathInline tex="2^{1-n}" />，但<strong>不含区间端点</strong>。
        实用上常改用<strong>极值点</strong>
        <MathInline tex="x_i = \cos(i\pi/(n-1))" />（含两个端点，本讲动画用的就是它），
        它的 <MathInline tex="\max|\prod|" /> 约为最优值的两倍
        （<MathInline tex="n=21" /> 时 <MathInline tex="1.90\times10^{-6}" /> 对
        <MathInline tex="9.54\times10^{-7}" />）。
        <strong>差两倍，但比等距好两百倍</strong>——所以工程上无所谓，含端点更方便。
      </p>
      <p>
        <strong>回味：这一节真正的教训不是"换一组点"。</strong>
        余项公式把误差拆成两个因子，<strong>一个你管不着（函数的高阶导数），
        一个完全由你决定（节点位置）</strong>。
        整整一个世纪里，大家只顾着把第一个因子往小里想（"多项式次数高了总该更准吧"），
        没人意识到自己一直在<strong>随手糟蹋</strong>第二个因子。
        <strong>数值分析的许多进步都是这个形状：不是找到更强的定理，
        而是认出了公式里那个一直没人碰的自由度。</strong>
      </p>
    </RevealBox>
    <p>
      既然病根是"奇点太近 + 节点摆错位置"，解药就有两条：其一，<strong>别再等距取点</strong>，
      改用<strong>切比雪夫点</strong>（两端加密、中间稀疏），它恰好抵消了收敛半径的限制，误差随次数稳稳下降；
      其二，干脆<strong>放弃一根高次多项式</strong>，改用<strong>分段低次</strong>的样条——这才是工程界的主流选择。
      下面的动画让你亲手在这两条路之间切换。
    </p>
    <QuizBox
      quiz-id="interpolation-q1"
      question="插值点取得越多，多项式就一定越贴近真函数吗？（说的是等距取点这种最自然的取法）"
      hint="回想龙格那张表：节点从 11 个加到 31 个，最大误差是往哪个方向走的？"
      :options="[
        { t: '会：每多钉一个点，曲线就被约束得更死，只可能离真相更近', why: '这正是烧了整整一百年的那个直觉。多一个点确实多钉死一处，可它同时把多项式次数也抬高一级——新添的那个自由度，在点与点之间可以尽情撒野。龙格 1901 年当场打脸：对 $1/(1+25x^2)$ 等距取点，11 个点最大误差 1.92，31 个点冲到 2388，而函数自己的取值只在 0 到 1 之间。' },
        { t: '会，只是收敛得慢，取到几百个点总归压得下去', why: '方向反了。等距节点下误差不是慢慢下降，而是随节点数指数级上涨：1.92（11 点）→ 59.8（21 点）→ 2388（31 点）。真取到几百个点，两端会飞到天上去，中间那一段反而白搭。' },
        { t: '不一定：等距取点时点越多、两端甩得越凶，11 点误差 1.92，31 点就到了 2388', why: '对。这就是龙格现象，也是这一讲的那记耳光：贴合已知点和逼近真函数，根本不是一回事。治法有两条——把等距点换成两端加密的切比雪夫点（同样 31 点，误差 0.00243），或者干脆别用一根高次多项式，改用分段低次的样条。' },
        { t: '不一定，但毛病出在龙格那个函数本身不够光滑', why: '$1/(1+25x^2)$ 在实轴上无穷次可导，光滑得挑不出一点毛病，这正是这个反例吓人的地方。病根一在它延拓到复平面后靠得很近的那对极点，二在等距节点让余项里的连乘因子在两端鼓成大包——两件事都跟它在实轴上光不光滑无关。' },
      ]"
      :answer="2"
    />

    <h2><span class="sec-no">叁</span>亲手制造龙格灾难，再一键平息它</h2>
    <RungeDemo />

    <h2><span class="sec-no">肆</span>严格定义</h2>
    <div class="definition">
      <div class="def-title">📐 插值多项式、余项与样条</div>
      <p>
        给定 <MathInline tex="n+1" /> 个互异节点 <MathInline tex="(x_i,y_i)" />，存在<strong>唯一</strong>次数
        <MathInline tex="\le n" /> 的多项式 <MathInline tex="p" /> 满足 <MathInline tex="p(x_i)=y_i" />
        （唯一性由范德蒙行列式非零保证）。若 <MathInline tex="y_i=f(x_i)" /> 且 <MathInline tex="f\in C^{n+1}" />，则<strong>插值余项</strong>为
      </p>
      <MathBlock tex="f(x)-p(x)=\frac{f^{(n+1)}(\xi)}{(n+1)!}\prod_{i=0}^{n}(x-x_i)" />
      <p>
        龙格现象的根源正在此式：等距节点下 <MathInline tex="\prod(x-x_i)" /> 在两端剧烈增大，
        而 <MathInline tex="f^{(n+1)}" /> 又不随 <MathInline tex="n" /> 收敛——两者相乘发散。切比雪夫点则是让
        <MathInline tex="\max\big|\prod(x-x_i)\big|" /> <strong>最小化</strong>的那组点。
      </p>
      <p>
        <strong>三次样条</strong>：不追求一根多项式全场通吃，而在每个小段上用三次多项式，段与段之间
        <strong>拼到二阶导都连续</strong>——既灵活又不震荡。名字来自造船厂：用一根有弹性的木条（样条 spline）
        压过一排钉子放样，木条自然弯成的正是这条最"省弯曲能量"的曲线。
      </p>
    </div>
    <QuizBox
      quiz-id="interpolation-q3"
      question="把等距点换成切比雪夫点之后，是不是次数越高就永远越准了？"
      hint="回头看余项公式：它是两个因子相乘，换节点位置动的是哪一个，另一个归谁管？"
      :options="[
        { t: '是：既然节点那个因子已经被压到理论下界，误差就只会一路变小', why: '余项是两个因子的乘积，换节点只把你能控制的那一个压到了最小；另一个因子 $f^{(n+1)}(\\xi)/(n+1)!$ 完全由函数自己决定，它可以涨得比前者掉得还快。切比雪夫点让「规矩的函数」收敛得极快，但没给所有函数发通行证。' },
        { t: '不是：切比雪夫点只治好了「节点摆错位置」这一条病根，函数自身那个因子仍不归你管；何况真实数据带噪声时高次多项式照样会去追噪声，所以工程主流仍是分段低次的样条', why: '对。本讲开的是两副药：换节点位置、改分段低次，缺一不可。前者在光滑函数上效果惊人（31 点误差 0.00243），可它没改变「一根高次多项式全场通吃」这件事。字体轮廓、CAD 曲面、船体放样用的全是分段三次，没有谁拿一根 50 次多项式去描一个字。' },
        { t: '不是，因为切比雪夫点算起来太贵，节点数一多就算不动了', why: '恰恰相反，$x_i=\\cos(i\\pi/(n-1))$ 是一行代码的事，比等距点也就多一次余弦调用，代价可以忽略。它的局限不在算得贵，在于它只按住了误差公式里的一个因子。' },
        { t: '不是，切比雪夫点只在 $[-1,1]$ 上有定义，换个区间就用不了了', why: '换区间只要做一次线性变换把 $[a,b]$ 映到 $[-1,1]$，节点跟着搬过去即可，这从来不是障碍。真正的限制是余项里另一个因子不归你管，以及真实数据里还有噪声这件事。' },
      ]"
      :answer="1"
    />

    <h2><span class="sec-no">伍</span>买到了什么：一条贯穿数字世界的曲线</h2>

    <h3>过拟合：机器学习的第一课，其实是 1901 年那个巴掌</h3>
    <p>
      本讲最值钱的东西不是插值技术，是那个被打碎的直觉：
      <strong>"模型越复杂、越贴合已知数据，就越接近真相"</strong>。
      一百年后，同一个巴掌在机器学习课的第一周又打了一次，
      名字换成了<strong>过拟合</strong>。而龙格现象是它<strong>最干净的数学原型</strong>——
      干净到可以当场把两条曲线算出来。
    </p>
    <p>
      <strong>做个实验。</strong>真相是一条正弦曲线
      <MathInline tex="y = \sin 2\pi x" />，但你拿不到它，
      只有 <strong>12 个带噪声的观测点</strong>（噪声 ±0.2）。
      用不同次数的多项式去最小二乘拟合，同时记两个数：
      <strong>训练误差</strong>（拟合曲线离那 12 个点多远）和
      <strong>测试误差</strong>（拟合曲线离<strong>真相</strong>多远）：
    </p>
    <MathBlock tex="\begin{array}{c|cc} \text{多项式次数} & \text{训练误差} & \text{测试误差} \\ \hline 1 & 0.522 & 0.472 \\ 3 & 0.124 & 0.095 \\ 5 & 0.104 & \mathbf{0.067} \\ 7 & 0.095 & 0.084 \\ 9 & 0.055 & 0.174 \\ 11 & \mathbf{0.043} & \mathbf{0.385} \end{array}" />
    <QuizBox
      quiz-id="interpolation-q2"
      question="先别往下看：训练误差最小的那个模型，是不是最好的模型？"
      hint="表里有两列，问问自己哪一列才对应「接近真相」这件事。"
      :options="[
        { t: '不是：11 次那一档训练误差最小（0.043），测试误差却是全场最大（0.385）', why: '对。11 次多项式穿过全部 12 个点，把每个点连同它身上 ±0.2 的噪声一起背了下来——训练误差理论上应当为零，可它离真相 $\\sin 2\\pi x$ 最远。该比的从来不是「见过的数据复述得多准」，而是「没见过的地方错多少」；按这把尺子，全场最好的是 5 次那一档（测试误差 0.067）。' },
        { t: '是：训练误差就是模型犯的错，犯得越少当然越好', why: '训练误差衡量的只是「把手里那 12 个点复述得多准」。而这 12 个点带着 ±0.2 的噪声，复述得越准，恰恰说明连噪声都被一字不落地背了下来。背书不等于学会：11 次那档训练误差 0.043 全场最低，测试误差 0.385 全场最高，差了将近九倍。' },
        { t: '看情况：只要数据量足够大，训练误差最小的就是最好的', why: '数据多确实能推迟过拟合（得更高的次数才背得下来），但换不掉判据本身——比的仍然是没见过的数据。而且龙格 1901 年那个反例连噪声都没有、点还随便加，照样是点越多越糟。这是逼近本身的性质，不是「数据不够多」能推脱的。' },
        { t: '不是，训练误差最小说明这个模型的次数取得太低了', why: '反了。次数越高，可调的自由度越多，训练误差只会更低——表里从 1 次到 11 次，训练误差单调地从 0.522 掉到 0.043。所以训练误差特别小恰恰是次数偏高的警报，不是偏低。' },
      ]"
      :answer="0"
    />
    <p>
      <strong>请把最后一行的两个粗体数放在一起看：训练误差最小的那个模型，
      测试误差是全场最大的——差了将近九倍。</strong>
      而它们是同一个模型。如果你只看训练误差（也就是"它把已知的点拟合得多好"），
      你会毫不犹豫地选中最差的那一个。
    </p>
    <p>
      <strong>而 11 次那一档正是本讲的插值。</strong>12 个点、11 次多项式——
      未知系数的个数恰好等于方程个数，多项式被<strong>精确地钉在每一个点上</strong>，
      训练误差理论上应当为零（这里 0.043 只是最小二乘求解时的数值残差）。
      <strong>换句话说：过拟合的极端，字面上就是插值。</strong>
      龙格 1901 年在一个<strong>没有噪声</strong>的完美函数上都做不到"点越多越准"，
      而真实数据还带着噪声——高次多项式会一丝不苟地去追那些噪声，
      把它们当成信号，于是在点与点之间放飞自我。
    </p>
    <div class="insight">
      <div class="insight-title">💡 三条解药，一一对应</div>
      <p>
        更有意思的是：本讲给出的两条解药，在机器学习里都能找到<strong>同一个东西的另一个名字</strong>。
      </p>
      <p>
        <strong>① 换节点位置（切比雪夫点）↔ 决定"去哪儿采数据"。</strong>
        上面那个折叠框的结论是：误差里有一项<strong>完全由采样位置决定</strong>，
        而大多数人从不去动它。机器学习里对应的是<strong>主动学习</strong>与实验设计——
        与其盲目多标一万条数据，不如挑那些最能减少不确定性的样本去标。
        <strong>"数据在哪儿"往往比"数据有多少"更值钱</strong>，这两门课给出的是同一句话。
      </p>
      <p>
        <strong>② 分段低次（样条）↔ 分段建模。</strong>
        与其用一根高次曲线全场通吃，不如在每个小区间上用简单模型再拼起来。
        决策树、随机森林、梯度提升树全是这个思路的变体；
        神经网络里的 ReLU 网络<strong>字面上就是一个分段线性函数</strong>——
        它之所以稳，理由和样条之所以不震荡是同一条。
      </p>
      <p>
        <strong>③ 而机器学习还多出一条本讲没有的：正则化。</strong>
        既然祸根是系数被逼得又大又互相抵消（高次插值多项式的系数常常大到
        <MathInline tex="10^3" /> 以上，再靠正负抵消凑出一条温和的曲线），
        那就<strong>在目标函数里直接惩罚系数的大小</strong>。
        岭回归干的就是这件事：在"拟合得好"之外，再给一整套系数的<strong>总体大小</strong>记一笔罚款——
        谁想靠冲天的系数去讨好那几个训练点，罚款就让它得不偿失。
        而这笔罚款在数值上还有第二重作用——
        <router-link to="/numerical/linear-system">第 5 讲</router-link>会看到，
        它同时把那个病态的方程组拉回良态。
      </p>
      <p>
        <strong>最后一句留给那个百年直觉。</strong>它错在哪儿？
        错在把"穿过已知点"当成了"接近真相"。
        <strong>已知点只是真相的有限证据，而模型越复杂，
        它能在证据之间胡说八道的自由度就越大。</strong>
        龙格用一个连噪声都没有的函数证明了这件事，
        所以它不是"数据不够好"能推脱的——<strong>这是逼近本身的性质。</strong>
      </p>
    </div>

    <h3>还买到了什么</h3>
    <ul>
      <li>
        <strong>屏幕上每一个字母</strong>：字体轮廓用的是贝塞尔曲线（样条的近亲）——
        你现在看到的每一个汉字，都是若干段三次曲线的控制点被存进字库、再由渲染器现算出来的。
        CAD、动画的关键帧插值、UI 动效的缓动曲线，本质都是"给几个控制点、要一条光滑曲线"。
        <strong>注意它们无一例外都是分段低次的</strong>——没有任何字体格式会用一根 50 次多项式
        去描一个字的轮廓，理由就是本讲第贰节那张脸；
      </li>
      <li>
        <strong>造船与工业设计</strong>：样条从船体放样起家，如今是所有曲面建模的基石——你的手机外壳、汽车车身，
        都是样条曲面拼出来的；
      </li>
    </ul>
    <div class="insight">
      <div class="insight-title">🔗 与你学过的课程连一连</div>
      <p>
        <strong>线性代数</strong>：求插值多项式的系数 = 解一个<strong>范德蒙线性方程组</strong>；而范德蒙矩阵是出了名的
        <em>病态</em>——这直接把你推向<router-link to="/numerical/linear-system">第五讲的条件数</router-link>，
        也是实际中改用拉格朗日 / 牛顿基而非直接解系数的原因；<strong>微积分</strong>：那个插值余项
        <MathInline tex="\tfrac{f^{(n+1)}(\xi)}{(n+1)!}\prod(x-x_i)" /> 和
        <router-link to="/calculus/taylor">泰勒余项</router-link>长得几乎一模一样——都是"高阶导数 × 一堆因子"在控制误差，
        插值是"多点贴合"、泰勒是"一点贴到底"，一对孪生兄弟；<strong>复变函数</strong>：龙格现象的真正裁判是复平面上那对极点——
        <router-link to="/complex/residue">收敛半径 = 到最近奇点的距离</router-link>在这里第二次收割。
      </p>
    </div>
  </ConceptPage>
</template>
