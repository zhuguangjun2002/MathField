# CLAUDE.md — 项目工作指引

## 项目是什么

「数学的来龙去脉」：把数学概念的**历史动机**讲清楚的中文网站。
用户会做题但觉得概念懵懂，本站补的是教科书缺的那一环——概念为什么被发明。

## 不可动摇的内容风格（用户明确选择）

每一讲必须按固定五段展开，节标题用 壹/贰/叁/肆/伍 编号：

1. **困境** —— 历史上遇到了什么具体麻烦（讲故事，给年代和人物）
2. **破局/思路** —— 为什么非发明这个概念不可，思想转折在哪
3. **交互动画** —— 不是演示而是论证：让用户拖滑杆亲手"发现"结论
4. **严格定义** —— 教科书定义，逐词拆解为什么这么写
5. **应用与联系** —— 尽量勾连用户学过的课（微积分、线性代数、概率论、复变函数、数值分析；**数学物理方程用户没学过**——本站数理方程课已按零基础写成，勾连它时优先指向站内对应讲，不要假设用户已有课堂背景）。跨课勾连尽量用站内 `<router-link>` 指向对应讲，让伏笔在站内真正兑现

风格类比 3Blue1Brown；语言是中文，正文口吻是"讲给一个会做题的大学毕业生"。

**这五段会悄悄退化，改完一讲要数一遍**（2026-08-05 微积分五讲全部中招）：
demo 被随手插在贰节末尾、第 3 段"交互动画"整个消失，四讲连编号都只排到"肆"。
自查一行命令：

```bash
grep -n 'sec-no' src/views/<course>/<X>View.vue | sed 's/<[^>]*>//g'
```

必须正好五条，且叁必须是动画节。标题口吻照复变那套：
「壹困境 / 贰破局 / **叁亲手…** / 肆严格定义 / 伍买到了什么」——
叁的标题一律以"亲手/亲眼"开头（亲手圈奇点、亲手切条、亲手把割线压成切线）。

**跨课链接是单向的，要主动回链**（同日）：后建的课链回了先建的课，先建的课一处都不链回去
（微积分被复变/数值分析链过来 7 次，自己只有 2 个 `router-link`，还都在一个折叠框里）。
写完一讲用 `grep -rn 'router-link' src/views/ | grep '/<本课 slug>/'` 数一遍别人链过来几次，
再回头看本讲兑现了几条。

### 不许只给结论（2026-07-25 用户连提五次的同一类问题）

复变第 1 讲一讲之内就被挑出五处，都是同一个毛病：**句子在陈述结果，读者却要的是那一步怎么迈过去的**。
写完每一段自查这几种"跳步"，见一个补一个：

- **等号从天而降**——"邦贝利硬算得 $\sqrt[3]{2+\sqrt{-121}} = 2+\sqrt{-1}$"。补：它是猜形状再立方回去验的，
  写出验算过程，并说清等号的含义是"验算通过"而非"解出来"
- **定义框只有条文**——"复数是有序实数对 $(a,b)$，记作 $a+bi$"。补：为什么改用实数对（动机）、
  $a+bi$ 这个写法是推出来的（实数嵌入 + $i=(0,1)$）、"构成一个域"要用逆元公式兑现
- **形容词代替推导**——"乘法公式不是拍脑袋""倍角和角公式成打掉落""最美的意外"。
  凡是这类夸奖，后面必须紧跟一次真的算给读者看
- **超前引用**——用本讲还没定义的东西（如 $e^{i\theta}$）去解释本讲的结论。要么明说"此处先当缩写、
  凭据在某讲"，要么补上定义
- **甩术语就跑**——"阻抗、相位、乘 ±i 的元件"这种外课黑话，站内又没有任何一讲讲过。
  要么翻译成人话（讲清需要哪几条前提，并说明**不需要**先学一学期那门课），要么删掉
- **新概念只报名字**（2026-08-05 补：用户"我不懂势、流场这些概念"）——"考虑一个定常的二维流场"
  "它保证存在一个速度势 $\varphi$"。这类词读者根本没学过，补一句形式定义远远不够。四步一套：
  ① 给一个能**贯穿全节的具体画面**（河心立一根桥墩，水绕过去——正好接上 demo 里"绕圆柱"那一档）；
  ② 讲这个概念**为什么值得发明**（一个数的函数就把整张箭头图定死了，方程少一半）；
  ③ 给两三个**跨学科的例子**（插座上的 220 V、地图等高线、重力势能）；
  ④ 给一个**反例**说明条件不能省（漩涡场没有全局的势，绕一圈对不上）。
  符号密集的推导（$\varphi$、$\lambda$、$\beta$、$y$ 一起上）还可以专做一张**符号示意图**：
  三面板把同一样东西在球面 / 局部小三角形 / 纸面上各指一遍，见 `MercatorVarsDemo.vue`
- **动画旋钮没有出处**（同日）——滑杆标签写着"厚度 %""弯度 %"，正文从没介绍过这几个名词，
  更没说 % 相对于什么。每个旋钮都要答得出三问：这个词什么意思、单位是什么、数字相对于谁。
  做法见下面"动画的旋钮要自带解释"
- **主对象没定义**（2026-08-05 复变第 3、4 讲各犯一次，是最隐蔽的一种）——第 3 讲整讲在讨论
  $\int_\Gamma f(z)\,dz$，却从没说过复积分是什么；第 4 讲整讲在用洛朗展开，也只甩了个式子。
  写完一讲**回头数一遍**：这一讲反复出现的那个主对象、那个主记号，本讲（或前面某讲）真的定义过吗？
  越是"看着眼熟"的记号越容易漏——$\int$ 长得跟实积分一样，就没人想起要说 $\Delta z$ 是个带方向的复数
- **跨讲复用的概念要在本讲原地请回来**（2026-08-06 复变第 3 讲，是"主对象没定义"的接缝版）——
  第 3 讲从第贰节起反复"代入柯西-黎曼的第一条/第二条"，可那两条方程只在**第 2 讲**定义过，
  本讲一次都没写出来。这比彻底没定义更隐蔽：`grep` 全站能搜到它，
  写的人于是觉得"讲过了"，读者却是顺着一讲往下读的，只见术语不见内容。
  自查：把本讲用到的每个概念列一遍，凡是**定义在别讲**的，本讲要么原地复述一次，
  要么给出 `<router-link>`——而**被反复代入的那种，必须原地复述**，
  不能只丢个链接叫人翻回去。复述四件事一件不少：怎么被逼出来的（一步推导）、
  记号约定（`u_x` 是 $\partial u/\partial x$）、**编号对得上**
  （正文说"第二条"，公式里就得用 `\underbrace` 标出哪条是第二条）、
  它的身份（C-R 不是额外假设，是"f 解析"的逐点翻译）。
  注意这类补充**不许收进 RevealBox**——读者是因为不认识它才卡住的
- **动机是空的**——第 5 讲通篇讲"延拓 ζ 有多重要"，可 ζ 和素数到底什么关系全讲未提，
  而那正是这一讲存在的理由（补欧拉乘积即可，三行）。自查：这一讲开头许诺的那个动机，
  正文里**兑现了吗**，还是只反复强调它很重要
- **伍节全是内部数学**（2026-08-06 用户："复变缺实际应用，很抽象"）——这条查不出来，
  因为逐讲复查是**在一讲之内**找跳步的，而"这一讲有没有一个真实世界的应用"是**整讲级**指标：
  五讲的伍节摆在一起才看得出洼地。复变那次的账是：第 1 讲有交流电相量、第 2 讲有机翼/墨卡托/流场，
  而第 3 讲整节是平均值性质 + 刘维尔 + 代数基本定理（**全是数学内部**），
  第 4 讲只甩了个形状 $\int f(\omega)e^{i\omega t}d\omega$ 说"这是工程方法的最后一公里"，
  **一个例子都没有**。自查：把本课每一讲的伍节标题与小节列出来排成一张表，
  逐讲问"这里有没有一件**课堂外真实发生**的事"；没有就补，而且要补成完整小节
  （画面 → 为什么绕道这门数学 → 推导 → **读出物理** → 条件不成立会怎样），不是加一条 li。
  补的时候优先挑**读者身上就有的**那件事（法拉第笼：车里被雷劈；阻尼振子：敲钟的余音），
  别挑"某某方程在某某领域的应用"这种只换了个名词的假应用
- **开篇端出的例子，正文要真算**（同日，留数讲）——第 4 讲开篇列了三道"物理学家的噩梦"积分
  当动机，正文却只算了 $\int dx/(1+x^2)$，而且自己承认"这道 arctan 也做得出来，是个体检题"，
  紧接着写"$\int_0^\infty \sin x/x\,dx$ 的原函数根本写不出来，这条流水线照样开工"——
  **说了但没做**。这是"该难的地方恰恰被跳过"的变体：动机里最唬人的那个例子，
  往往正是正文最想绕开的那个。自查：**开篇摆出来当动机的每一个具体例子，回头数一遍算了几个**
- **前后指代落空**——第 5 讲第贰节写着"回到开篇那句'胡话'"，可开篇根本没有那句话
  （多半是早先删改留下的断茬）。凡"上面说过""开篇那个""前面提到的"，回去核一眼真有没有。
  **最容易新造出断茬的时刻，是你自己挪动小节顺序之后**（2026-08-06 一次改稿连造两处：
  在伍节开头插入新小节、把原来的 `<ul>` 挤到后面，于是"本节第一条要说的""本节开头说的"
  两句同时失效）。插入或挪动小节后，把被挪走那段里的指代词全部重查一遍
- **原稿里的经验数字多半没人算过**（2026-08-05 微积分讲，两处）——泰勒讲写着"再算七八项多项式，
  误差就低于浮点精度"（实测 6 项就和标准库一位不差），极限 demo 的 note 写着"ε=0.01 就要 N=100"
  （实际 101）。这类数字读者最容易照着试，一试就露馅。**改稿时把原文里每一个具体数字都当作可疑，
  一律用 `node -e` 重算**——顺带常有意外收获：那次复算才发现 $\sum i^2$ 的裂项推导、
  阿基米德"每轮 1/4"的三次方律都是白给的，正好一并补掉
- **该难的地方恰恰被跳过**（同日，基本定理讲最典型）——那一讲的立论是"按定义算积分有多难"，
  可举例时 $\sum i^2$ 的求和公式和取极限两步全被"要写出……再取极限才得到 1/3"一句带过。
  自查：**这一讲用来立论的那个"难"，正文里真的演示了吗**，还是只是断言它难

### 长推导默认收起（用 RevealBox）

补上的推导不能一上来就摊开——那等于替读者把思考做完了。超过三四段的推导一律塞进 `RevealBox`：
折叠时只露标题和一句**思考提示**，提示要给一个**能动笔的具体任务**（"把这两个复数乘开，
再想想左边按辐角相加等于什么"），按钮文案写成"对答案 / 看完整拆解"，让读者自己推完再点开核对。
结论本身留在框外，藏起来的只有过程。

**但概念解释不许收起**——上面那套"新概念只报名字"的四步（画面 / 动机 / 例子 / 反例）
要摊在正文里。读者是因为看不懂才需要它，藏进折叠框等于没写。RevealBox 只收**推导过程**。

**`title` 是纯文本 prop，塞不了 KaTeX**（`hint` 才有 `#hint` 具名插槽）。所以标题里别写
$\oint$、$\int$、$\sqrt{\ }$ 这类符号——写了会被 `scan-math-katex.py` 报出来，
而且渲染出来就是个裸字符。改用文字描述（"绕小圈积 $(z-z_0)$ 的 n 次幂"），或把符号挪进正文。

### 动画的旋钮要自带解释

滑杆是读者第一个上手的东西，也最容易变成黑话。三处一起补，缺一处仍然懵：

1. **标签自带括注**——`厚度 %（圆心左移）`、`迎角 °（来流仰角）`、`变形进度 %（圆→翼型）`。
   括注要短：`.ctrl` 的 min-width 是 240px、桌面宽两个一行，标签一长就把滑杆轨道挤没了
2. **读数区给实时换算**——别在正文里写死一个系数，让 demo 当场把滑杆读数折成读者认得的量：
   "右图 弦长 = 4.05，最大厚度 = 14% 弦长，最大拱高 = 3.5% 弦长"。这一行直接回答"% 相对于什么"
3. **`#note` 开头逐条列出每个旋钮**——讲清长度尺子取在哪（"原点到临界点 $z=1$ 为 1"）、
   哪些是真物理参数、哪些**只是动画进度**（茹科夫斯基那个"变形进度"就不是机翼参数，必须点破）

正文里 demo 之前还要加一句引导：RevealBox 是折叠的，读者很可能跳过推导直接去拖滑杆。

**`#note` 里叫读者"这样拖"的每一组配置，都要真拖一遍（或用 `node -e` 算一遍）**。
留数 demo 的 note 曾写着"圈住 z=1 和 z=−1 得 3"——可那三个极点到原点的距离都是 1，
光调半径它们同进同出，**照着做永远做不到**，得先挪圆心。这种错读者一试就撞上，比甩结论更伤。
现在那条 note 给的是五组验算过的具体坐标（`(0, −0.3, R=1.1)` 这种），照着拖必定复现。

**正文管什么叫"灵魂/关键/主角"，读数区就得有它的数字**（2026-08-05 泰勒讲）。
那一讲白纸黑字写着"余项 Rₙ 才是定理的灵魂"，可 `TaylorDemo` 的 readout 只印多项式，
读者拖半天也量不到余项。补法是给 demo 挑一个固定**检验点**（这里取 x = 2），
当场算出"多项式的值 / 真值 / 两者之差"三个数——顺带白送一个反面档：
`ln(1+x)` 的收敛半径是 1，检验点落在圈外，阶数越高余项越大
（n=15 时给出 1424，真值 1.0986），比正文讲十遍"出了圈就发散"都管用。

**note 想讲的那个状态，demo 里必须真的到得了——到不了就改 demo，不是改 note**
（2026-08-05 线代第 4 讲）。`SpanDemo` 的 note 写着"把两个角度都拖到 0，
看张成从直线跳变成平面"，可 $v_3$ 的平面内方向是写死的常量，$\alpha = 0$ 时它仍指着别处，
**读数最低只到 2，秩 1 在这个 demo 里根本拖不出来**。这类错和"note 写错配置"不同：
配置没写错，是<strong>参数化本身让教学点不可达</strong>。改法是动 demo（让 $v_3$ 的水平分量跟着
$\alpha$ 转），而不是把 note 改成迁就现状——**动画是拿来论证的，论证不到就是设计漏了**。

**判定类 demo 都有容差，容差要写进 `#note`**（同日，线代第 4、5 讲各一处）。
凡是 demo 里出现"共线了""秩掉了""达标了"这种<strong>是非判断</strong>，代码里必然有个阈值
（`SpanDemo` 判秩用 0.12、`EigenDemo` 判共线用 0.035）。后果有两个，都得写给读者：
① **跳变会比理论上晚**——秩要多拖约 2° 才跳；② **判定区间的边缘，读数是假的**——
剪切矩阵那档绿色区间两边各宽出七八度，边缘报出的 $\lambda$ 会漂到 1.1，只有正中心那个 1 是真的。
顺带说清"为什么必须有阈值"（现实数据带噪声，"是不是零"没法用等号问），
读者就不会以为程序算错了。

**拖拽式 demo 没有滑杆，`#note` 照样要"逐条列控件"**（同日，`DeterminantDemo`）。
它的控件是两个可以直接拖的箭头，原 note 从没说过这件事，读者可能压根没发现能拖。
写法照旧：红箭头是哪一列、蓝箭头是哪一列、读数的颜色各代表什么。
量宽时也要特别对待——见下面"量宽脚本"那条。

**别在读数区断言刀刃上的不等号**（同日，极限讲踩到）。原本想写
`|a_N − L| = 0.0250 < ε = 0.025 ✅`，可振荡数列那档 ε = 0.025 时误差恰好是 1/40，
**数学上正好压线**，四舍五入后显示成"0.0250 < 0.025"，读者一眼就觉得是假的；
而且加显示位数救不了（试到 7 位仍然撞车，因为它真的相等）。
改成给**相邻两项的对照**——"第 N 项 0.1250 已进带，前一项 0.2500 还在带外，所以这个 N 已经最小"——
信息更多，还绕开了边界。凡是 readout 里要写 `<`、`>`、`✅` 的地方，
先拿 `node -e` 把滑杆的每一档都扫一遍，看有没有哪档会显示成假话。

**量宽脚本只量默认那一档旋钮，最宽的那档得自己扫**（同日）。
`measure-readout.mjs` 打开页面就量，可 readout 最长的往往是某个极端档位
（`TaylorDemo` 是 `eˣ` + n=15，`ln(1+x)` 又贡献了最长的数字 1424.4246；
`DeterminantDemo` 拖出负数时会在 820px 处溢出 1px，默认状态完全看不出来）。
改完带动态数字的 readout，起一个 playwright 把状态走一遍再量：

- **有下拉 + 滑杆的**：下拉逐项 × 每个滑杆取 min/中/max 做笛卡尔积。
  线代那轮扫到 86 种不同读数才敢说没问题
- **靠鼠标拖的**（`DeterminantDemo`）：`page.mouse` 那套<strong>不一定触发得了</strong>，
  组件监听的是 pointer 事件。直接 `page.evaluate` 里手工
  `dispatchEvent(new PointerEvent('pointerdown'/'pointermove'/'pointerup', {pointerId: 1, ...}))`

**脚本必须自证"状态真的变了"**——这是那轮最值钱的一条教训。
第一版扫描脚本因为选错了容器，滑杆一次都没动过，却一路打印"全部档位均不溢出"，
差点就信了。现在的写法是把每次读到的 readout 文本收进一个 `Set`，
**结尾打印"扫到 N 种不同的读数文本"**：N = 1 就说明脚本在空转，报告一律作废。
凡是"没发现问题"的自动检查，都要先能证明它确实检查过。

## 技术栈与结构

Vue 3 + Vite + vue-router（hash 路由）+ KaTeX。动画是手写 Canvas，无第三方图表库。

```
src/data/courses.js        课程/概念元数据（单一事实来源，新增先改这里）
src/components/            MathInline, MathBlock（KaTeX）、RichText（把字符串里 $...$ 交给
                           KaTeX，其余当 HTML；courses.js 的文案字段专用）、
                           DemoFrame（动画卡片框架）、
                           ControlSlider（滑杆）、ConceptPage（概念页统一布局+上下讲导航）、
                           RevealBox（先思考后揭晓：折叠时只露标题+一句思考提示 hint，
                           点按钮才展开推导；hint 里要写公式就改用 #hint 具名插槽）
src/demos/plot.js          Canvas 绘图工具：usePlot(自适应+高分屏+watch重绘)、
                           makeView(坐标映射)、makeSquareView(等比例视图，几何/变换类动画必用)、
                           makeFitView(等比例且装下指定数据框；配 pad 可把画布切成"原像/像"双面板，
                           两边给同样 span 才不会看起来偷偷缩放)、clipView(双面板必用)、
                           sampleGrid + contourSegments(marching squares 等值线；只发一小段一小段
                           不连折线，故意的——端点可再过一次映射，同一族线就同时画到了两个平面)、
                           drawAxes、plotFn、drawPoint、drawLabel、drawArrow、调色板 C
src/demos/*.vue            每个动画一个组件，套 DemoFrame，控件放 #controls，
                           实时数值放 #readout，说明文字放 #note
src/views/concepts/*.vue   微积分每讲一个页面，套 ConceptPage（传 slug）
src/views/linear-algebra/  线性代数每讲一个页面（同样套 ConceptPage）
src/views/probability/     概率论每讲一个页面；模拟类动画用 plot.js 的 rng(seed) 保证可复现
src/views/complex/         复变函数每讲一个页面（复数运算在各 demo 内手写展开为实虚部）
src/views/mathphys/        数学物理方程每讲一个页面（用户零基础，行文最细、推导放慢）
src/views/numerical/       数值分析每讲一个页面；动画多为可复现的实算（浮点抵消、牛顿迭代、
                           龙格插值、求积、条件数），部分 demo 真用 float64 当场算出误差
docs/course-plans.md       各课程蓝图（讲次、动画构想）——六门课 30 讲已全部上线
```

## 新增一讲的流程

1. `src/data/courses.js` 登记元数据（slug/title/subtitle/question/era/figures）
2. `src/demos/` 写动画组件（复用 plot.js，参考 RiemannSumDemo.vue 最典型）
3. `src/views/concepts/` 写内容页（参考 LimitView.vue 的结构和样式类）
4. `src/router.js` 加路由
5. 验证：`npx vite build` 必须过；起 dev server 后用无头 Chrome 截图检查渲染
   （`google-chrome --headless=new --no-sandbox --screenshot=... --window-size=1280,4200 --virtual-time-budget=8000 <url>`）

## 新开一门课的流程

（原规划五门课 + 后加的数值分析共六门课已全部上线；此流程适用于将来增开新课或补选讲，如复变第 5 讲提过的 ζ 函数短讲。数值分析是范例：不在原 course-plans.md 蓝图里，也照此流程从零建了一门课。）

1. 先读 `docs/course-plans.md` 里该课的规划（讲次划分、每讲动画构想已写好）
2. `courses.js` 里把该课 `available: false` 改为 true，补 `path`、`concepts` 数组，
   以及课程总览页数据 `intro`（可含 HTML）、`timelineTitle`、`timeline`、`tocNote`
   —— courses.js 里凡出现公式，一律写成 `$...$`（LaTeX，JS 字符串里反斜杠要转义成 `\\`），
   不要用 `√` `²` `∫` 这类 Unicode 字符；已接 RichText 的字段：课程 `intro`、
   时间线 `title`/`text`、概念 `subtitle`/`question`
3. 路由、视图目录按已有课程的模式复制（每门课建 `src/views/<course>/` 子目录）
4. CourseView、ConceptPage、顶部导航均已数据驱动（courses.js 是唯一事实来源），无需改组件；
   概念 slug 需全站唯一（conceptContext 按 slug 跨课程查找）

## 样式约定

- 全局样式都在 `src/styles/main.css`，CSS 变量定义配色（暖纸色底、朱红强调、靛蓝定义框）
- 内容页可用的样式类：`.story`（历史故事，金色）、`.definition`（严格定义，靛蓝）、
  `.insight`（直觉/联系，朱红）；正文 `strong` 会自动变朱红
- 动画配色用 plot.js 导出的 `C`：曲线主体用 `C.ink`，重点/可动元素用 `C.accent`，
  目标/参考用 `C.indigo`，辅助标注用 `C.gold`，达标状态用 `C.green`

## Canvas 画图的三个坑（2026-08-05 一次踩全）

新写 demo 后，**务必把每个滑杆拖到两端、组合四个角**再看一遍——默认参数下好看不算数。

- **文字要画在 `clipView` 之外**。贴边的标签（三角形直角边的边名、轴名）在极端参数下会紧挨面板
  边界，被裁掉且毫无提示（墨卡托符号图里 `R dφ` 就这么消失过）。形状在 `ctx.save()/clipView`
  里画，`ctx.restore()` 之后再 `drawLabel`
- **角弧、标注的像素半径要随图形缩放**。写死 34px 的角弧，在图形被压扁时会比图形本身还大。
  用 `Math.max(下限, Math.min(34, 该图形的像素高 × 0.55))`；标签位置也跟着这个半径走
- **分箱统计要让采样点远密于箱数**。按 x 分箱量轮廓的上下缘时，若每箱摊不到几个点，就会有箱
  只落到上表面，"中线"被抬高、算出的量凭空变大（茹科夫斯基读数一度报出 9.7% 的假拱高，
  真值 3.5%）。3000 采样点 / 120 箱是安全量级。**读数区里任何现算的量，都要另拿一段
  `node -e` 独立算一遍对答案**

## 公式书写约定

数学符号在页面上有三种渲染路径，按所在位置选对写法，别混：

- **正式公式**（view 里独立成行或行内的算式）：用 `<MathBlock>` / `<MathInline tex="..." />`（KaTeX）
- **`courses.js` 的文案字段**（intro、时间线 title/text、概念 subtitle/question）：写成 `$...$`，
  由 RichText 交给 KaTeX（JS 字符串里反斜杠转义成 `\\`）
- **渲染给用户看的散文里**（view 的 `<p>`、demo 的 `#note`/`#readout`）：出现下面任一，一律改用
  `<MathInline>`（demo 组件如未 import 需补 `import MathInline from '../components/MathInline.vue'`）：
  ① **带脱字符的指数**（`e^(−n²π²τ)`、`e^{...}`）——`^`、括号会原样显示，像坏公式；
  ② **露出下划线的下标**（`u_xx`、`b_m`、`Z_n`、`z_k`）——同上，且常常同一段里紧挨着的
     `<MathInline tex="u_{xx}" />` 已经排得好好的，对比之下更刺眼；
  ③ **任何根式**（`√n`、`√2`、`√−1`、`√(T/ρ)`）——裸 `√` **没有横线盖住被开方数**，
     `√n` 看着像"√ 和 n 两个字"。
  全站体检：`python3 scripts/scan-math-katex.py`（只扫 `<template>`，已抠掉 Math* 标签与 `{{ }}` 插值；
  剩下报出来的多是刻意保留的 Unicode 大算符与"讨论符号本身"的句子，逐条看一眼即可）
- **保留 Unicode 内联**的两种情形：①简单单字符上下标（`x²`、`n²`、`t₀`、`aₙ`）是全站刻意的轻量风格
  ——**但根号与大算符不在此列**，见上条（2026-07-25 全站散文与 readout 里的裸 `√`、`∫ ∮ ∂`
  已清零，别再引入）；②canvas 的 `drawLabel`/`yLabel`/`xLabel` 字符串——canvas 画不了 KaTeX，
  Unicode 是唯一写法
- **唯一该留裸符号的情形**：句子在**讨论这个符号本身**（"计算器芯片不认识'√'这个符号"、
  "所以才共用一个 `∫` 记号"、"`∂` 这个弯尾巴的 d"）。此外站标那个 `∫` 是 logo，不是公式。

## 读数区（`#readout`）不能超宽

`.demo-readout` 是 `white-space: nowrap` + `overflow-x: auto`：**一超宽就变成横向滚动条**，
右边的内容读者根本不知道存在（曾经偏导数 demo 的"两者之比"被挤出去，而紧接着的 `#note`
正文还写着"读数区里那个比值始终在 1 附近"）。改完 readout（尤其把 Unicode 换成 KaTeX，会变宽）必量：

```bash
node scripts/measure-readout.mjs                      # 全站，1280px 桌面宽，只报溢出的
node scripts/measure-readout.mjs /calculus/derivative  # 单页，逐档 1440→390px
```

目标：**1024px 以上必须为 0，820px 尽量为 0**（640/390 手机宽放不下任何一条读数，是全站通病，暂不处理）。
超宽了怎么办，按这个顺序：① 删冗余前缀与括注（"[t₀, t₀+h] **上的**平均速度"→"[t₀, t₀+h] 平均速度"）；
② 重复结构压缩（"两个 ½ac 三角形（合 ac = 3）"→"½ac 三角形×2 = 3"）；
③ 信息不能删时（如各项配色与画布图块一一对应）**插 `<br>` 断行**——`nowrap` 只挡自动换行，`<br>` 照断。

## 公式块（`MathBlock`）同样不能超宽

`.math-block` 是 `overflow-x: auto`，超宽就只剩一条细滚动条，公式右半截读者看不见
（曾经"用乘积卡住 a、b"那条的 `= ∛125 = 5` 就这么被吃掉过）。新写的推导步骤最容易踩，改完必量：

```bash
node scripts/measure-mathblock.mjs                   # 全站，1280px 桌面宽，只报溢出的
node scripts/measure-mathblock.mjs /calculus/taylor  # 单页，逐档 1440→390px
```

脚本会自动点开页面上所有 `RevealBox`（折叠着的公式否则量不到）。目标同读数区：
**1024px 以上必须为 0，820px 尽量为 0**（640/390 手机宽放不下长公式，全站通病，暂不处理）。
超宽了：① 用 `\begin{aligned}` 按 `&=` 断行；② 把公式尾巴上的中文注释（`\text{（辐角相加）}`）挪回正文；
③ 断不开就拆成两个 MathBlock。另：多条级数竖排对齐时用 `\phantom{...}` 占位，
能让 cos/sin 的项正好落在 $e^x$ 对应幂次的正下方（泰勒讲欧拉公式那段就靠这个才看得出"偶次/奇次分工"）。

## 常用命令

```bash
npm run dev      # 开发
npx vite build   # 构建验证（改完必跑）

# 体检（起 dev server 后跑，读数区/公式块两个脚本默认连 5191 端口：
#   npm run dev -- --port 5191 --strictPort；三个都在 scripts/）
python3 scripts/scan-math-katex.py       # 散文里漏掉 KaTeX 的公式（^ / _ / √ / 大算符）
node scripts/measure-readout.mjs         # demo 读数区横向溢出
node scripts/measure-mathblock.mjs       # 独立成行公式横向溢出（会自动点开 RevealBox）
```
